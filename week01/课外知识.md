记一个刷leetCode得到的知识，
求一个[Number]数组所有的子集，
可以把每个索引上的数转化为当前数组长度的二进制
如 [1, 2]
可以分为 [], [1], [2], [1, 2]
转化后就是 0, 10 , 01 , 11
最大数2  为 2 的 length 幂
循环0 - 2 右移 0 - length 的长度
首位为1就是结果
关键代码
```javascript
[0 , Math.pow(2, arr.length) ] >>> [0 , length] & 1 === 1
```